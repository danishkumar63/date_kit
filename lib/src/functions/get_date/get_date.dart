/// Returns the day of the month from the given [date].
///
/// [date]: The date for which the day of the month is to be determined.
///
/// Returns the day of the month as an integer.
int getDate(
  DateTime date,
) {
  return date.day;
}
