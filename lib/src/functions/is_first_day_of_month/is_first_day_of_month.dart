/// Determines if the given [date] is the first day of the month.
///
/// [date]: The date to be checked.
///
/// Returns true if [date] is the first day of the month, otherwise false.
bool isFirstDayOfMonth(
  DateTime date
) {
  return date.day == 1;
}
