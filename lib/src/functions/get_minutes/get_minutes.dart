/// Returns the minute component of the given [date].
///
/// [date]: The date for which the minute component is to be determined.
///
/// Returns an integer representing the minute component.
int getMinutes(
  DateTime date,
) {
  return date.minute;
}
